# Lab 07 — Digital signatures / certificates

> Not started.

## Task

Build a certificate (name + RSA public key), sign its SHA-256 fingerprint with your RSA private key, and sign a timestamped fingerprint SHA256(SHA256(cert) || ts16). Reuse the lab 6 RSA key.

Full assignment text: [`task.txt`](task.txt). Original brief: the `ISP_L7_*.pdf` in this folder.

Variant = student ID number mod 10 + 1.

## Files

- `ISP_L7_Skaitmeninis_parašas.pdf`
- `pagalbines_funkcijos.py`
- `task.txt` — assignment text (transcribed)

## Notes

_TBD_
